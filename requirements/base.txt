anthropic==0.43.0
asgiref==3.11.1
Django==5.2.11
django-cors-headers==4.9.0
django-environ==0.12.1
django-filter==25.2
django-vite==3.1.0
djangorestframework==3.16.1
djangorestframework_simplejwt==5.5.1
drf-spectacular==0.29.0
httpx==0.28.1  # Telegram Bot API client (already an anthropic dependency)
gunicorn==21.2.0
inflection==0.5.1
jsonschema==4.26.0
jsonschema-specifications==2025.9.1
openpyxl==3.1.5
packaging==26.0
pgvector==0.5.0  # Django VectorField + pgvector k-NN (parcours 21). Server extension installed via migration.
pillow==12.1.1
pillow-heif==0.18.0
psycopg==3.3.2
psycopg-binary==3.3.2
PyJWT==2.11.0
pypdf==5.1.0
pypdfium2==4.30.1
pywebpush==2.3.0  # Web Push (VAPID) delivery — parcours mobile. Pulls cryptography/py-vapid/http-ece.
PyYAML==6.0.3
referencing==0.37.0
rpds-py==0.30.0
# Génération des QR d'étiquettes de zone (parcours 31). Python pur, sans
# extension C : le rendu reste côté serveur plutôt que d'ajouter une dépendance
# JS pour un écran ouvert une fois par foyer.
segno==1.6.6
sqlparse==0.5.5
typing_extensions==4.15.0
uritemplate==4.2.0
whitenoise==6.11.0
# Brotli — whitenoise ne produit des .br qu'avec lui. Le bundle React tombe de
# 900 Ko à ~215 Ko, contre ~257 Ko en gzip, et le travail est fait une fois au
# build plutôt qu'à chaque requête.
Brotli==1.1.0
