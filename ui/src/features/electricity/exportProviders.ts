/**
 * Page where a French user downloads their consumption export (Enedis).
 * Used as a placeholder / suggestion only — the URL actually opened is the one
 * saved on the meter (`export_url`), since it can be a supplier portal instead.
 */
export const ENEDIS_EXPORT_URL = 'https://mon-compte-particulier.enedis.fr/donnees/';
